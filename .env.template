# =============================
# Template de fichier .env pour l'application RAG
# Copiez ce fichier en .env et remplissez les valeurs nécessaires
# =============================

# Clé API OpenAI (OBLIGATOIRE)
# Obtenez votre clé sur https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# User-Agent personnalisé pour les requêtes web (OPTIONNEL)
# Améliore la compatibilité avec certains sites web
USER_AGENT=RAG-ExampleBot/1.0

# Configuration Qdrant distante (OPTIONNEL)
# Décommentez et configurez si vous utilisez une instance Qdrant externe
# QDRANT_HOST=localhost
# QDRANT_PORT=6333

# Configuration des modèles (OPTIONNEL)
# Vous pouvez personnaliser les modèles utilisés
# OPENAI_MODEL=gpt-4o-mini
# EMBEDDING_MODEL=text-embedding-3-small

# Configuration de la température (OPTIONNEL)
# Contrôle la créativité des réponses (0.0 = déterministe, 1.0 = très créatif)
# TEMPERATURE=0.1

# Configuration des chunks (OPTIONNEL)
# Taille des chunks pour le découpage des documents
# CHUNK_SIZE=1000
# CHUNK_OVERLAP=200

# =============================
# INSTRUCTIONS D'UTILISATION
# =============================
# 1. Copiez ce fichier : cp .env.template .env
# 2. Ouvrez .env et remplacez les valeurs par vos vraies clés
# 3. Ne partagez JAMAIS votre fichier .env
# 4. Le fichier .env est déjà dans .gitignore pour votre sécurité
# =============================
